{{#if id}}
  <a class="logout" href="/logout">Log out</a>
  {{#if pictureUrl}}
    <img title="{{firstName}} {{lastName}}" class="topbar-photo" src="{{pictureUrl}}">
  {{/if}}
{{else}}
  <a href="/auth/linkedin">Log in</a></li>
{{/if}}
