<a class="button tiny secondary useThisToSearch" data-historyindex="{{historyIndex}}" href="#">
  {{#if jobTitle}}
    <span>{{jobTitle}}</span>
  {{/if}}
  {{#if company}}
    <span>{{company}}</span>
  {{/if}}
  {{#if jobKeywords}}
    <span>{{jobKeywords}}</span>
  {{/if}}
  {{#if distance}}
    <span>{{distance}} miles</span>
  {{/if}}
</a>

